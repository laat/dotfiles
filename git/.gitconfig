[user]
	name = Sigurd Fosseng
	email = sigurd@fosseng.net
[diff]
	algorithm = histogram
	colorMoved = default
[color]
	diff = auto
	status = auto
	branch = auto
	ui = true
[alias]
	# Resolves the default branch from the cached refs/remotes/origin/HEAD (no
	# network). Note: `git fetch` does not refresh that ref, so if the remote's
	# default branch is renamed, run `git remote set-head origin -a` once.
	main = "!f() { \
		b=$(git symbolic-ref refs/remotes/origin/HEAD --short 2>/dev/null) \
		|| b=$(git remote set-head origin -a >/dev/null 2>&1 && git symbolic-ref refs/remotes/origin/HEAD --short 2>/dev/null) \
		|| b=$(git remote show origin | sed -n '/HEAD branch/s/.*: //p'); \
		[ -n \"$b\" ] || { echo 'git main: could not determine default branch' >&2; exit 1; }; \
		git checkout \"${b#origin/}\"; \
	}; f"
	m = main
	co = checkout
	ci = commit
	cis = commit -S
	st = status
	br = branch
	l = log --stat --abbrev-commit
	ls = log --pretty=format:"%C(yellow)%h%Cred%d\\ %Creset%s%Cblue\\ [%cn]" --decorate
	ll = log --pretty=format:"%C(yellow)%h%Cred%d\\ %Creset%s%Cblue\\ [%cn]" --decorate --numstat
	lol = log --graph --decorate --pretty=oneline --abbrev-commit --all
	d = diff
	dc = diff --cached
	panic = !tar cvf ../git_panic.tar *
	changelog = !git-changelog
	ignore = "!gi() { curl -L -s https://www.gitignore.io/api/$@ | sed '/^# .*toptal/d' ;}; gi"
	cai = !git-aia
	ai = !git-ai
[include] # silent fail
	path = /etc/gitconfig
	path = .gitconfig.osx
	path = .gitconfig.linux
	path = .gitconfig.local
	path = ~/.gitconfig.local
[push]
	default = simple
	autoSetupRemote = true
[core]
	autocrlf = input
	excludesfile = ~/.gitignore_global
[url "https://github.com/"]
	# GitHub over HTTPS instead of SSH, whatever the remote URL says. An SSH
	# fetch/pull/push paid ~1.9s of handshake; ControlMaster brought that to
	# ~0.7s but its master leaks the stderr pipe when the remote command fails
	# (fetching a branch that does not exist, which gh-stack does for every new
	# branch), so anything that captures git's output hangs for good. HTTPS is
	# ~0.9s per operation with no master to wedge. Keeps wm-add (cmd+shift+t)
	# snappy since it fetches before workmux add. Commit signing uses the
	# 1Password SSH key and is unaffected; that is not transport.
	insteadOf = git@github.com:
	insteadOf = ssh://git@github.com/
[credential "https://github.com"]
	# After osxkeychain (from /opt/homebrew/etc/gitconfig): gh's keyring token
	# when the keychain has nothing, so an expired PAT never prompts.
	helper = !gh auth git-credential
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[magit]
	hideCampaign = true
[diff "sopsdiffer"]
	textconv = sops -d
[github]
	user = laat
[diff "orgmode"]
	xfuncname = "^(\\*+.*)$"
[merge]
	conflictstyle = zdiff3
[rerere]
	enabled = true
[fetch]
	prune = true
[log]
	abbrevCommit = true
[branch]
	sort = -committerdate
[pull]
	rebase = true
[init]
	defaultBranch = main	
